# Versions that can unpickle the pretrained models in data/models/.
# The classifiers were saved with scikit-learn 0.22.1. The RandomForest (cNEU)
# uses a decision-tree node layout that changed in scikit-learn 1.3, so it
# only loads on scikit-learn < 1.3. That build in turn needs numpy < 2.
scikit-learn==1.2.2
numpy<2
scipy<1.13
